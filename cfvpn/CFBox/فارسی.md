# مدیر اشتراک CFBox

> **تغییر زبان**  [简体中文](README.md)  [English](English.md)  [فارسی](فارسی.md)

> **پنل مدیریت اشتراک پروکسی که روی Cloudflare Workers / Pages اجرا می‌شود** — ترکیبی از قابلیت‌های CFnew و edgetunnel (cmliu)؛ هم می‌تواند در مسیر `/UUID` (یا مسیر سفارشی) اشتراک چندپروتکلی VLESS / Trojan / xhttp تولید کند و هم **پنل پیکربندی گرافیکی دوحالته** (حالت استاندارد / حالت پیشرفته با تعویض یک‌کلیکی)، **تولید اشتراک بهینه** (با ۲۶ منبع بهینه داخلی)، اشتراک چندکلاینت، تست شبکه و ذخیره‌سازی KV با اعمال فوری را ارائه می‌دهد.
---
> **[گروه تلگرام](https://t.me/SZ_PAI)**

> **[کانال یوتیوب](https://www.youtube.com/@PAI_CN)**
---
## بهروزرسانیها (v1.0 → v1.1)

### گسترش منابع برتر داخلی
- منابع برتر داخلی از **۲۶ به ۳۲** افزایش یافت
- منابع منطقهای bestcf از **۵ منطقه به ۱۱ منطقه** گسترش یافت؛ جدید: KR، DE، SE، NL، FI، GB

### رفع اشکال
- رفع باگی که پس از مشخصکردن یک منطقه خاص، همچنان همه نودها ارسال میشدند

### چندمنبعی شدن تشخیص منطقه IP
- **v1.0**: تکمنبع `ping0.cc`
- **v1.1**: منبع اصلی `ping0.cc` + منبع جایگزین `ipinfo.io`، با بازگشت خودکار

### سازگاری
- موارد پیکربندی، رابطهای API، پروتکلها (VLESS / Trojan / xhttp)، پنل / چندزبانه / کلاینتها و موارد دیگر **همگی بدون تغییر باقی میمانند**
- برای ارتقا نیازی به تغییر KV و متغیرهای محیطی نیست

## ۱. نمای کلی پروژه و سازگاری

| مورد | محتوا |
|---|---|
| نام پروژه | **مدیر اشتراک CFBox** |
| محیط اجرا | Cloudflare Workers / Pages |
| فرم استقرار | Worker تک‌فایلی (`CFBox明文版.js` / `CFBox混淆版.js`) |
| ذخیره‌سازی داده | Cloudflare KV (متغیر اتصال **K**، سازگار با C/KV/ConfigKV/CFKV/CFBOX) |
| حالت‌های پنل | حالت استاندارد + حالت پیشرفته (تعویض یک‌کلیکی در گوشه بالا راست؛ حالت پیشرفته با پس‌زمینه سبک نئون) |

### ۱.۱ سازگاری با IP / نودهای CFnew و edgetunnel

CFBox هنگام تولید اشتراک، منابع IP / نود هر دو پروژه CFnew و edgetunnel (cmliu) را یکپارچه ادغام می‌کند؛ IPهای هر دو سیستم به‌صورت بومی سازگار بوده و می‌توانند همزمان استفاده شوند.

| منبع | توضیح |
|---|---|
| **IPهای CFnew** | ① فهرست دامنه‌های بهینه به‌صورت داخلی (`cloudflare.182682.xyz`، `bestcf.top` و...) <br>② بهینه آنلاین (فیلتر IPv4/IPv6 بر اساس اپراتور) <br>③ IP بهینه سفارشی `yx` با فرمت `IP:Port#Name` |
| **IPهای edgetunnel (cmliu)** | ① کتابخانه دامنه‌های پروکسی معکوس ProxyIP به‌صورت کامل داخلی، به‌عنوان آدرس پشتیبان / رله <br>② سازگار با زنجیره fallback `GO2SOCKS5` / SOCKS5 |

هنگام تولید اشتراک، آدرس‌های اتصال مستقیم رسمی، دامنه‌های بهینه، IPهای بهینه آنلاین، منابع بهینه داخلی / سفارشی، بهینه مخزن و فهرست پروکسی معکوس ProxyIP همگی ادغام و خروجی می‌شوند؛ IPv4 / IPv6 / دامنه / نام مستعار `#Name` همگی قابل تجزیه هستند. اتصال مستقیم رسمی به‌طور پیش‌فرض **بیش از ۸۰۰ نود** ارائه می‌دهد (منابع بهینه داخلی + تکمیل تصادفی CIDR رسمی Cloudflare) و می‌توان از طریق «تولید اشتراک بهینه» نودهای بیشتری اضافه کرد.

---

## ۲. ویژگی‌های اصلی

| # | ویژگی | توضیح |
|---|---|---|
| ۱ | **پشتیبانی چندپروتکل** | VLESS / Trojan / xhttp، قابل فعال‌سازی همزمان با کلید یک‌کلیکی در رابط گرافیکی |
| ۲ | **پنل دوحالته** | حالت استاندارد (ماژول‌های رایج به‌صورت فشرده) + حالت پیشرفته (تنظیمات سفارشی / کنترل‌های پیشرفته)، تعویض یک‌کلیکی در گوشه بالا راست، حفظ یکپارچگی رابط و عملکرد |
| ۳ | **تولید اشتراک بهینه** | سه حالت: تولید اشتراک بهینه (مخصوص مبتدیان) / حالت بهینه تصادفی (بهینه رسمی) / حالت اشتراک سفارشی (پشتیبانی از تجمیع)، پیش‌فرض «حالت اشتراک سفارشی»، ۲۶ منبع بهینه داخلی |
| ۴ | **انواع بهینه داخلی** | کلیدهای مستقل: آدرس بومی (`ena`) / دامنه بهینه (`epd`) / IP بهینه (`epi`) / بهینه مخزن (`egi`) |
| ۵ | **تنظیمات فیلتر IP بهینه** | انتخاب نسخه IP (IPv4 / IPv6) + فیلتر اپراتور (موبایل / یونیکام / تلکام) |
| ۶ | **تست شبکه** | تست یک‌کلیکی استریم/هوش مصنوعی (Google / Netflix / Disney+ / HBO / HBOMax / Peacock / GitHub / GPT / Gemini) + تست سرعت یک‌کلیکی نود فعلی (fiber.google.com) |
| ۷ | **پیکربندی گرافیکی** | پیکربندی ذخیره‌شده در KV، اعمال فوری پس از تغییر (اتصال به **K**) |
| ۸ | **مدیریت API** | افزودن/حذف RESTful IPهای بهینه (`/api/preferred-ips`) |
| ۹ | **چندکلاینت** | CLASH / SURGE / SING-BOX / LOON / QUANTUMULT X / V2RAY / Shadowrocket / STASH / NEKORAY / V2RAYNG، شناسایی خودکار بر اساس UA |
| ۱۰ | **چندزبانه** | چینی / فارسی / انگلیسی، تعویض خودکار بر اساس زبان مرورگر + تعویض از منوی کشویی گوشه بالا راست |
| ۱۱ | **ECH** | Encrypted Client Hello |
| ۱۲ | **تنظیمات سفارشی** | URL صفحه اصلی سفارشی (`homepage`) / مسیر سفارشی / ProxyIP سفارشی در یک ماژول ادغام شده |
| ۱۳ | **پارامترهای مسیر کلاینت** | پوشش تک‌نودی `p` / `wk` / `rm` / `s` |

---

## ۳. راهنمای استقرار

### استقرار روی Worker

۱. وارد کنسول Cloudflare شوید ← Workers و Pages ← Worker جدید بسازید
۲. `CFBox混淆版.js` (نسخه ساده) را در کد Worker قرار دهید
۳. **متغیر محیطی `U`** = UUID شما (الزامی؛ U بزرگ، سازگار با `UUID` / u کوچک) تنظیم کنید
۴. یک **فضای نام KV** ایجاد کرده و در تنظیمات Worker آن را متصل کنید؛ نام متغیر را **`K`** بگذارید (اصلی؛ سازگار با `C`/`KV`/`ConfigKV`/`CFKV`/`CFBOX`)
۵. پس از استقرار، برای ورود به پنل پیکربندی گرافیکی به آدرس `https://دامنه-شما/UUID` مراجعه کنید

### استقرار روی Pages

۱. وارد کنسول Cloudflare شوید ← Workers و Pages ← پروژه Pages جدید بسازید
۲. `CFBox混淆版.js` را به `_worker.js` تغییر نام دهید، آن را فشرده (zip) کرده و آپلود کنید (یا پوشه حاوی `_worker.js` را بکشید و رها کنید)
۳. **متغیر محیطی `U`** = UUID شما (الزامی؛ U بزرگ، سازگار با `UUID` / u کوچک) تنظیم کنید
۴. یک **فضای نام KV** ایجاد کرده و در تنظیمات Pages آن را متصل کنید؛ نام متغیر را **`K`** بگذارید (اصلی؛ سازگار با `C`/`KV`/`ConfigKV`/`CFKV`/`CFBOX`)
۵. پس از استقرار، برای ورود به پنل پیکربندی گرافیکی به آدرس `https://دامنه-شما/UUID` مراجعه کنید

### فرآیند ورود

از `/` بازدید کنید ← صفحه ورود ترمینال ← UUID (یا مسیر سفارشی) را وارد کنید ← «ورود موفق» ← وارد پنل شوید.

---

## ۴. جدول متغیرهای محیطی و متغیرهای پیکربندی

> اولویت: **پیکربندی KV > متغیرهای محیطی > مقادیر پیش‌فرض**

### ۴.۱ پیکربندی پایه

| متغیر | مقدار | توضیح |
|---|---|---|
| `U` | UUID شما | **الزامی**، برای دسترسی به رابط اشتراک و پیکربندی (U بزرگ، سازگار با `UUID` / u کوچک) |
| `p` | proxyip | اختیاری، آدرس و پورت ProxyIP سفارشی (IPv4/IPv6/دامنه). با تنظیم آن، تطبیق منطقه `wk` غیرفعال می‌شود (انحصاری) |
| `s` | آدرس SOCKS5 | اختیاری، `user:pass@host:port` یا `host:port` |
| `d` | مسیر سفارشی | اختیاری، مثلاً `/mypath` یا `/path/to/sub`؛ در صورت خالی بودن از مسیر UUID استفاده می‌شود |
| `wk` | کد منطقه | اختیاری، تعیین دستی منطقه Worker (SG/HK/US/JP...). پس از تنظیم `p` غیرفعال می‌شود (انحصاری) |
| `homepage` | URL صفحه اصلی سفارشی | اختیاری، هنگام بازدید از مسیر ریشه `/` این URL به‌عنوان صفحه پوششی نمایش داده می‌شود |

### ۴.۲ پیکربندی پروتکل

| متغیر | مقدار | توضیح |
|---|---|---|
| `ev` | yes/no | فعال‌سازی VLESS (پیش‌فرض فعال) |
| `et` | yes/no | فعال‌سازی Trojan (پیش‌فرض غیرفعال) |
| `ex` | yes/no | فعال‌سازی xhttp (پیش‌فرض غیرفعال) |
| `tp` | رمز سفارشی | رمز Trojan؛ در صورت خالی بودن از UUID استفاده می‌شود |
| `ech` | yes/no | فعال‌سازی ECH (پیش‌فرض غیرفعال؛ پس از فعال‌سازی، حالت فقط TLS به‌صورت خودکار فعال می‌شود) |

### ۴.۳ کنترل‌های پیشرفته (حالت پیشرفته)

| متغیر | مقدار | توضیح |
|---|---|---|
| `yx` | IP بهینه سفارشی | پشتیبانی از نام‌گذاری، `1.1.1.1:443#Hong Kong Node,8.8.8.8:53#Google DNS` |
| `yxURL` | URL منبع بهینه | منبع فهرست IP سفارشی (در صورت خالی بودن از منبع پیش‌فرض داخلی استفاده می‌شود) |
| `scu` | آدرس تبدیل اشتراک | آدرس سرویس تبدیل اشتراک (پیش‌فرض `https://url.v1.mk/sub`) |
| `epd` | yes/no | فعال‌سازی دامنه‌های بهینه (پیش‌فرض فعال) |
| `epi` | yes/no | فعال‌سازی IPهای بهینه (پیش‌فرض فعال) |
| `egi` | yes/no | فعال‌سازی بهینه مخزن (پیش‌فرض فعال) |
| `ena` | yes/no | فعال‌سازی آدرس‌های بومی (پیش‌فرض غیرفعال) |
| `qj` | no | تنظیم بر `no` برای فعال‌سازی fallback: شکست اتصال مستقیم CF ← SOCKS5 ← fallback |
| `dkby` | yes | تنظیم بر `yes` برای تولید فقط نودهای TLS |
| `yxby` | yes | تنظیم بر `yes` برای غیرفعال‌سازی تمام عملکردهای بهینه |
| `rm` | no | تنظیم بر `no` برای غیرفعال‌سازی تطبیق هوشمند منطقه |
| `ae` | yes | تنظیم بر `yes` برای اجازه مدیریت API (پیش‌فرض غیرفعال) |

> فیلتر IP بهینه (`ipv4` / `ipv6` / `ispMobile` / `ispUnicom` / `ispTelecom`)، DNS سفارشی (`customDNS`، فرمت DoH)، دامنه ECH سفارشی (`customECHDomain`)، مذاکره ALPN (`alpn`) و... همگی در ماژول‌های «انواع بهینه داخلی»، «تنظیمات فیلتر IP بهینه» و «کنترل‌های پیشرفته» در پنل قابل پیکربندی هستند (پس از اتصال KV نمایش داده می‌شوند).

---

## ۵. توضیح عملکرد پنل (حالت استاندارد / پیشرفته)

پس از استقرار، برای ورود به پنل پیکربندی گرافیکی از `/{UUID}` بازدید کنید. در گوشه بالا راست **دکمه تعویض حالت** وجود دارد؛ با کلیک می‌توانید بین «حالت استاندارد» و «حالت پیشرفته» جابه‌جا شوید.

### ۵.۱ حالت استاندارد (چیدمان فشرده ماژول‌های رایج)

| ماژول | توضیح |
|---|---|
| **مدیریت پیکربندی** | پس از اتصال KV نمایش داده می‌شود، ذخیره همه پیکربندی‌ها (اتصال به K) |
| **وضعیت سیستم** | نمایش منطقه Worker، روش تشخیص، ProxyIP، IP فعلی و... |
| **تولید اشتراک بهینه** | حالت‌های اشتراک بهینه (خاموش / تولید اشتراک بهینه / بهینه تصادفی / اشتراک سفارشی)، ۲۶ منبع بهینه داخلی، شامل عملکردهای «شروع بهینه»، «API اشتراک»، «پروکسی زنجیره‌ای» |
| **انتخاب کلاینت** | ۱۰ کلاینت (CLASH / SURGE / SING-BOX / LOON / QUANTUMULT X / V2RAY / Shadowrocket / STASH / NEKORAY / V2RAYNG)، تولید لینک اشتراک و راه‌اندازی برنامه |
| **انواع بهینه داخلی** | کلیدهای مستقل: آدرس بومی (`ena`) / دامنه بهینه (`epd`) / IP بهینه (`epi`) / بهینه مخزن (`egi`) |
| **تنظیمات فیلتر IP بهینه** | فیلتر نسخه IP (IPv4 / IPv6) + اپراتور (موبایل / یونیکام / تلکام) |
| **تست شبکه** | تست یک‌کلیکی استریم/هوش مصنوعی + تست سرعت یک‌کلیکی نود فعلی |
| **پیکربندی مسیر فعلی** | نمایش نوع استفاده / مسیر فعلی / آدرس دسترسی |
| **لینک‌های مرتبط** | لینک‌های خارجی پرکاربرد (مخزن، انجمن و...) |

### ۵.۲ حالت پیشرفته (تنظیمات سفارشی + کنترل‌های پیشرفته)

| ماژول | توضیح |
|---|---|
| **تنظیمات سفارشی** | URL صفحه اصلی سفارشی (`homepage`) / مسیر سفارشی (`d`) / ProxyIP سفارشی (`p`) در یک ماژول ادغام شده |
| **کنترل‌های پیشرفته** | آدرس تبدیل اشتراک (`scu`) / اجازه مدیریت API (`ae`) / تطبیق منطقه (`rm`) / روش خروج (`qj`) / کنترل TLS (`dkby`) / کنترل بهینه (`epd`/`epi`/`egi`/`ena`/`yxby`) |

### ۵.۳ تعویض حالت و پس‌زمینه

- دکمه «تعویض حالت» در گوشه بالا راست، تعویض یک‌کلیکی بین حالت استاندارد ↔ حالت پیشرفته
- حالت پیشرفته از **پس‌زمینه سبک نئون** استفاده می‌کند و چیدمان ماژول‌ها در هر دو حالت بازچینی شده و رابط و عملکرد یکسان حفظ می‌شود

---

## ۶. تولید اشتراک بهینه و بهینه محلی

### ۶.۱ حالت‌های اشتراک بهینه

| حالت | توضیح |
|---|---|
| خاموش | استفاده از منطق پیش‌فرض تولید اشتراک |
| **تولید اشتراک بهینه (مخصوص مبتدیان)** | وارد کردن دامنه تولیدکننده، فراخوانی خودکار API اصلی تولید بهینه edgetunnel |
| **حالت بهینه تصادفی (بهینه رسمی)** | تولید تصادفی IPهای بهینه از CIDR رسمی Cloudflare بر اساس تعداد مشخص (`subRandomCount`) |
| **حالت اشتراک سفارشی (پشتیبانی از تجمیع)** (پیش‌فرض) | تجمیع منبع‌به‌منبع از «بهینه سفارشی (هر خط یک‌عدد)»، پشتیبانی از دامنه / IPv4 / IPv6 / API بهینه `sub://` / API بهینه `https://` / منابع بهینه داخلی (در حالت اشتراک سفارشی ۲۶ عدد پیش‌فرض) |

### ۶.۲ حالت بهینه محلی
> رابط API مورد استفاده روش‌های بهینه با پروژه [CFnew](https://github.com/byJoey/cfnew) یکسان است؛ می‌توانید مستقیماً از ابزار بهینه [yx-tools](https://github.com/byJoey/yx-tools) استفاده کنید؛ نتایج بهینه را می‌توان «دانلود» کرد یا از طریق «گزارش یک‌کلیکی» مستقیماً به پروژه گزارش داد.

### ۶.۳ سایر پارامترها

| متغیر | توضیح |
|---|---|
| `subRandomCount` | تعداد بهینه تصادفی (پیش‌فرض ۱۶) |
| `subPort` | پورت بهینه مشخص شده (۱- = پورت تصادفی) |
| `subName` | نام اشتراک (نام اشتراک تحویل‌شده به هر کلاینت در زیر «انتخاب کلاینت» به‌صورت همزمان اعمال می‌شود) |
| `subUpdateTime` | زمان به‌روزرسانی اشتراک (ساعت، پیش‌فرض ۳) |

---

## ۷. بهینه‌سازی عملکرد و امنیت

- **بهینه‌سازی خواندن KV**: کش حافظه ۳۰ ثانیه‌ای، اعتماد کامل به کش در بازه کوتاه، کاهش فشار درخواست‌های پرتکرار به KV
- **بهینه خودکار هر ۱۵ دقیقه**؛ طرح‌های جایگزین متعدد، کش هوشمند
- **مسدودسازی درخواست‌های نامعتبر**: مسیرهای غیرمجاز مستقیماً ۴۰۴ برمی‌گردانند بدون اینکه خواندن KV فعال شود
- **احراز هویت UUID**: دسترسی فقط زمانی مجاز است که UUID موجود در مسیر با متغیر `U` مطابقت داشته باشد (بدون حساسیت به حروف بزرگ/کوچک)
- **زنجیره fallback**: شکست اتصال مستقیم CF ← SOCKS5 ← fallback، افزایش قابلیت اطمینان

---

## ۸. قدردانی

- الهام‌گرفته از [byJoey/cfnew](https://github.com/byJoey/cfnew) / [cmliu/Edgetunnel](https://github.com/cmliu/edgetunnel)
- رابط‌های بهینه / منابع بهینه: bestcf، WeTest و سایر موارد نگهداری‌شده توسط انجمن
- تشخیص منطقه IP: ping0.cc
- تست سرعت شبکه: [Google Speedtest](https://fiber.google.com/speedtest/)
